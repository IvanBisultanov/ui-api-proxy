/**
 * Booking
 * Resources and methods to manage guest journeys.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

/* tslint:disable:no-unused-variable member-ordering */

import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/map';

import { Inject, Injectable, LOCALE_ID }                           from '@angular/core';
import { Http, Headers, URLSearchParams, Response, RequestMethod } from '@angular/http';

import * as models                                                 from '../model/models';
import { BASE_PATH, COLLECTION_FORMATS }                           from '../../variables';
import { ApaleoRequestOptions, ResponseModel }                     from '../../models';
import { Configuration }                                           from '../../configuration';
import { callApiEndpoint }                                         from '../../functions.api';



export namespace bookingBookingsByIdGet {
    export interface Params {
        /**
        * Id of the booking to be retrieved.
        */
        id: string;
    }
    
}
export namespace bookingBookingsPost {
    export interface Params {
        /**
        * The list of reservations you want to create.
        */
        requestBody: models.CreateBookingModel;
    }
    
}

@Injectable()
export class BookingApi {
    public readonly defaultHeaders: Headers = new Headers();

    constructor(
        protected readonly http: Http, 
        protected readonly configuration: Configuration,
        @Inject(LOCALE_ID) protected readonly locale: string, 
        @Inject(BASE_PATH) protected readonly basePath: string) {

        this.defaultHeaders.append('Accept-Language', locale);
    }

    /**
     * Returns a specific booking.
     * Retrieves a booking with all its reservations.
     * @param id Id of the booking to be retrieved.
     */
    public bookingBookingsByIdGet(params: bookingBookingsByIdGet.Params, $options?: ApaleoRequestOptions)
        : Observable<models.BookingModel | undefined> {
        return this.bookingBookingsByIdGetWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }

    /**
     * Creates a booking for multiple reservations.
     * Creates a booking taking multiple reservations as input
     * @param requestBody The list of reservations you want to create.
     */
    public bookingBookingsPost(params: bookingBookingsPost.Params, $options?: ApaleoRequestOptions)
        : Observable<models.BookingCreatedModel | undefined> {
        return this.bookingBookingsPostWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }


    /**
     * Returns a specific booking.
     * Retrieves a booking with all its reservations.
     * @param id Id of the booking to be retrieved.
     */
    public bookingBookingsByIdGetWithRawHttp(params: bookingBookingsByIdGet.Params, $options?: ApaleoRequestOptions)
        : Observable<ResponseModel<models.BookingModel>> {
        return this.bookingBookingsByIdGetWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }

    /**
     * Creates a booking for multiple reservations.
     * Creates a booking taking multiple reservations as input
     * @param requestBody The list of reservations you want to create.
     */
    public bookingBookingsPostWithRawHttp(params: bookingBookingsPost.Params, $options?: ApaleoRequestOptions)
        : Observable<ResponseModel<models.BookingCreatedModel>> {
        return this.bookingBookingsPostWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }


    /**
     * Returns a specific booking.
     * Retrieves a booking with all its reservations.
     * @param id Id of the booking to be retrieved.
     */
    private bookingBookingsByIdGetWithHttpInfo(params: bookingBookingsByIdGet.Params, $options?: ApaleoRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/booking/bookings/${id}'
                    .replace('${' + 'id' + '}', String(params.id));

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'id' is not null or undefined
        if (params.id === null || params.id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling bookingBookingsByIdGet.');
        }
        // to determine the Content-Type header
        let consumes: string[] = [
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain',
            'application/json',
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Get,
                headers: headers,
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.bookingBookingsByIdGetWithHttpInfo(params, $options);
            }
        )
    }

    /**
     * Creates a booking for multiple reservations.
     * Creates a booking taking multiple reservations as input
     * @param requestBody The list of reservations you want to create.
     */
    private bookingBookingsPostWithHttpInfo(params: bookingBookingsPost.Params, $options?: ApaleoRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/booking/bookings';

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'requestBody' is not null or undefined
        if (params.requestBody === null || params.requestBody === undefined) {
            throw new Error('Required parameter requestBody was null or undefined when calling bookingBookingsPost.');
        }
        // to determine the Content-Type header
        let consumes: string[] = [
            'application/json',
            'text/json',
            'application/json-patch+json'
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain',
            'application/json',
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        headers.set('Content-Type', 'application/json');

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Post,
                headers: headers,
                body: params.requestBody == null ? '' : params.requestBody, // https://github.com/angular/angular/issues/10612
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.bookingBookingsPostWithHttpInfo(params, $options);
            }
        )
    }

}
