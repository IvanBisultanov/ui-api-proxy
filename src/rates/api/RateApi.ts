/**
 * Rates Management
 * Manage the rate plans and rates to rent out your inventory at the best price.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import { Inject, Injectable, LOCALE_ID }                           from '@angular/core';
import { Http, Headers, URLSearchParams, Response, RequestMethod } from '@angular/http';

import { Observable }                                              from 'rxjs/Rx';
import 'rxjs/add/operator/map';

import * as models                                                 from '../model/models';
import { BASE_PATH, COLLECTION_FORMATS }                           from '../../variables';
import { IRequestOptions, ResponseModel }                          from '../../models';
import { Configuration }                                           from '../../configuration';
import { callApiEndpoint }                                         from '../../functions.api';

/* tslint:disable:no-unused-variable member-ordering */


export interface ratesRatePlansByIdRatesGetParams {
    /**
     * The id of the rate plan
     */
    id: string;
}
export interface ratesRatePlansByIdRatesPutParams {
    /**
     * The id of the rate plan.
     */
    id: string;
    /**
     * The definition of the rates.
     */
    requestBody: models.ReplaceRatesModel;
}

@Injectable()
export class RateApi {
    public defaultHeaders: Headers = new Headers();

    constructor(
        protected readonly http: Http, 
        protected readonly configuration: Configuration,
        @Inject(LOCALE_ID) protected readonly locale: string, 
        @Inject(BASE_PATH) protected readonly basePath: string) {

        this.defaultHeaders.append('Accept-Language', locale);
    }

    /**
     * Returns a list of rates.
     * Returns all rates for a specific rateplan.
     * @param id The id of the rate plan
     */
    public ratesRatePlansByIdRatesGet(params: ratesRatePlansByIdRatesGetParams, $options?: IRequestOptions)
        : Observable<models.RateListModel | undefined> {
        return this.ratesRatePlansByIdRatesGetWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }

    /**
     * Replaces the rates for the rate plan
     * Use this call to modify the rates of the rate plan.
     * @param id The id of the rate plan.
     * @param requestBody The definition of the rates.
     */
    public ratesRatePlansByIdRatesPut(params: ratesRatePlansByIdRatesPutParams, $options?: IRequestOptions)
        : Observable<void> {
        return this.ratesRatePlansByIdRatesPutWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }


    /**
     * Returns a list of rates.
     * Returns all rates for a specific rateplan.
     * @param id The id of the rate plan
     */
    public ratesRatePlansByIdRatesGetWithRawHttp(params: ratesRatePlansByIdRatesGetParams, $options?: IRequestOptions)
        : Observable<ResponseModel<models.RateListModel>> {
        return this.ratesRatePlansByIdRatesGetWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }

    /**
     * Replaces the rates for the rate plan
     * Use this call to modify the rates of the rate plan.
     * @param id The id of the rate plan.
     * @param requestBody The definition of the rates.
     */
    public ratesRatePlansByIdRatesPutWithRawHttp(params: ratesRatePlansByIdRatesPutParams, $options?: IRequestOptions)
        : Observable<ResponseModel<void>> {
        return this.ratesRatePlansByIdRatesPutWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }


    /**
     * Returns a list of rates.
     * Returns all rates for a specific rateplan.
     * @param id The id of the rate plan
     */
    private ratesRatePlansByIdRatesGetWithHttpInfo(params: ratesRatePlansByIdRatesGetParams, $options?: IRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/rates/rate-plans/${id}/rates'
                    .replace('${' + 'id' + '}', String(params.id));

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'id' is not null or undefined
        if (params.id === null || params.id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling ratesRatePlansByIdRatesGet.');
        }
        // to determine the Content-Type header
        let consumes: string[] = [
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain', 
            'application/json', 
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Get,
                headers: headers,
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.ratesRatePlansByIdRatesGetWithHttpInfo(params, $options);
            }
        )
    }

    /**
     * Replaces the rates for the rate plan
     * Use this call to modify the rates of the rate plan.
     * @param id The id of the rate plan.
     * @param requestBody The definition of the rates.
     */
    private ratesRatePlansByIdRatesPutWithHttpInfo(params: ratesRatePlansByIdRatesPutParams, $options?: IRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/rates/rate-plans/${id}/rates'
                    .replace('${' + 'id' + '}', String(params.id));

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'id' is not null or undefined
        if (params.id === null || params.id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling ratesRatePlansByIdRatesPut.');
        }
        // verify required parameter 'requestBody' is not null or undefined
        if (params.requestBody === null || params.requestBody === undefined) {
            throw new Error('Required parameter requestBody was null or undefined when calling ratesRatePlansByIdRatesPut.');
        }
        // to determine the Content-Type header
        let consumes: string[] = [
            'application/json', 
            'text/json', 
            'application/json-patch+json'
        ];

        // to determine the Accept header
        let produces: string[] = [
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        headers.set('Content-Type', 'application/json');

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Put,
                headers: headers,
                body: params.requestBody == null ? '' : JSON.stringify(params.requestBody), // https://github.com/angular/angular/issues/10612
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.ratesRatePlansByIdRatesPutWithHttpInfo(params, $options);
            }
        )
    }

}
