/**
 * Apaleo Finance API
 * Everything around the Folio, Invoices and Accounting.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

/* tslint:disable:no-unused-variable member-ordering */

import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/map';

import { Inject, Injectable, LOCALE_ID }                           from '@angular/core';
import { Http, Headers, URLSearchParams, Response, RequestMethod } from '@angular/http';

import * as models                                                 from '../model/models';
import { BASE_PATH, COLLECTION_FORMATS }                           from '../../variables';
import { ApaleoRequestOptions, ResponseModel }                     from '../../models';
import { Configuration }                                           from '../../configuration';
import { callApiEndpoint }                                         from '../../functions.api';



export namespace financeAccountsByNumberGet {
    export interface Params {
        /**
        * The account number
        */
        number: string;
        /**
        * Specifies the property the folio should be located in.
        */
        propertyId?: string;
    }
    
}
export namespace financeAccountsGet {
    export interface Params {
        /**
        * Filter account list by property id
        */
        propertyId: string;
        /**
        * Filter account list by the parent account's number.
        */
        parent: string;
    }
    
}
export namespace financeAccountsSchemaGet {
    export interface Params {
        /**
        * Specifies the property the folio should be located in.
        */
        propertyId?: string;
    }
    
}

@Injectable()
export class SubLedgerApi {
    public readonly defaultHeaders: Headers = new Headers();

    constructor(
        protected readonly http: Http, 
        protected readonly configuration: Configuration,
        @Inject(LOCALE_ID) protected readonly locale: string, 
        @Inject(BASE_PATH) protected readonly basePath: string) {

        this.defaultHeaders.append('Accept-Language', locale);
    }

    /**
     * Returns one account.
     * Returns one account, as specified by its number and the property.
     * @param number The account number
     * @param propertyId Specifies the property the folio should be located in.
     */
    public financeAccountsByNumberGet(params: financeAccountsByNumberGet.Params, $options?: ApaleoRequestOptions)
        : Observable<models.FinanceAccountModel | undefined> {
        return this.financeAccountsByNumberGetWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }

    /**
     * Returns a list of accounts.
     * Returns a list of accounts in one property, having the same parent.
     * @param propertyId Filter account list by property id
     * @param parent Filter account list by the parent account&#39;s number.
     */
    public financeAccountsGet(params: financeAccountsGet.Params, $options?: ApaleoRequestOptions)
        : Observable<models.FinanceAccountListModel | undefined> {
        return this.financeAccountsGetWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }

    /**
     * Returns the chart of accounts of the subledger.
     * Returns the schema of the property&#39;s subledger, split into global and guest ledger. The global ledger includes  all statically created accounts.
     * @param propertyId Specifies the property the folio should be located in.
     */
    public financeAccountsSchemaGet(params: financeAccountsSchemaGet.Params, $options?: ApaleoRequestOptions)
        : Observable<models.ChartOfAccountsModel | undefined> {
        return this.financeAccountsSchemaGetWithRawHttp(params, $options)
            .map(response => response.$hasValue(response) ? response : undefined);
    }


    /**
     * Returns one account.
     * Returns one account, as specified by its number and the property.
     * @param number The account number
     * @param propertyId Specifies the property the folio should be located in.
     */
    public financeAccountsByNumberGetWithRawHttp(params: financeAccountsByNumberGet.Params, $options?: ApaleoRequestOptions)
        : Observable<ResponseModel<models.FinanceAccountModel>> {
        return this.financeAccountsByNumberGetWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }

    /**
     * Returns a list of accounts.
     * Returns a list of accounts in one property, having the same parent.
     * @param propertyId Filter account list by property id
     * @param parent Filter account list by the parent account&#39;s number.
     */
    public financeAccountsGetWithRawHttp(params: financeAccountsGet.Params, $options?: ApaleoRequestOptions)
        : Observable<ResponseModel<models.FinanceAccountListModel>> {
        return this.financeAccountsGetWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }

    /**
     * Returns the chart of accounts of the subledger.
     * Returns the schema of the property&#39;s subledger, split into global and guest ledger. The global ledger includes  all statically created accounts.
     * @param propertyId Specifies the property the folio should be located in.
     */
    public financeAccountsSchemaGetWithRawHttp(params: financeAccountsSchemaGet.Params, $options?: ApaleoRequestOptions)
        : Observable<ResponseModel<models.ChartOfAccountsModel>> {
        return this.financeAccountsSchemaGetWithHttpInfo(params, $options)
            .map((response: Response) => new ResponseModel(response));
    }


    /**
     * Returns one account.
     * Returns one account, as specified by its number and the property.
     * @param number The account number
     * @param propertyId Specifies the property the folio should be located in.
     */
    private financeAccountsByNumberGetWithHttpInfo(params: financeAccountsByNumberGet.Params, $options?: ApaleoRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/finance/accounts/${number}'
                    .replace('${' + 'number' + '}', String(params.number));

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'number' is not null or undefined
        if (params.number === null || params.number === undefined) {
            throw new Error('Required parameter number was null or undefined when calling financeAccountsByNumberGet.');
        }
        if (params.propertyId !== undefined) {
            queryParameters.set('propertyId', <any>params.propertyId);
        }

        // to determine the Content-Type header
        let consumes: string[] = [
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain',
            'application/json',
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Get,
                headers: headers,
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.financeAccountsByNumberGetWithHttpInfo(params, $options);
            }
        )
    }

    /**
     * Returns a list of accounts.
     * Returns a list of accounts in one property, having the same parent.
     * @param propertyId Filter account list by property id
     * @param parent Filter account list by the parent account&#39;s number.
     */
    private financeAccountsGetWithHttpInfo(params: financeAccountsGet.Params, $options?: ApaleoRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/finance/accounts';

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        // verify required parameter 'propertyId' is not null or undefined
        if (params.propertyId === null || params.propertyId === undefined) {
            throw new Error('Required parameter propertyId was null or undefined when calling financeAccountsGet.');
        }
        // verify required parameter 'parent' is not null or undefined
        if (params.parent === null || params.parent === undefined) {
            throw new Error('Required parameter parent was null or undefined when calling financeAccountsGet.');
        }
        if (params.propertyId !== undefined) {
            queryParameters.set('propertyId', <any>params.propertyId);
        }

        if (params.parent !== undefined) {
            queryParameters.set('parent', <any>params.parent);
        }

        // to determine the Content-Type header
        let consumes: string[] = [
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain',
            'application/json',
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Get,
                headers: headers,
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.financeAccountsGetWithHttpInfo(params, $options);
            }
        )
    }

    /**
     * Returns the chart of accounts of the subledger.
     * Returns the schema of the property&#39;s subledger, split into global and guest ledger. The global ledger includes  all statically created accounts.
     * @param propertyId Specifies the property the folio should be located in.
     */
    private financeAccountsSchemaGetWithHttpInfo(params: financeAccountsSchemaGet.Params, $options?: ApaleoRequestOptions): Observable<Response> {
        params = params || {};
        const path = this.basePath + '/finance/accounts/schema';

        let queryParameters = new URLSearchParams();
        let headers = new Headers(this.defaultHeaders.toJSON()); // https://github.com/angular/angular/issues/6845
        if (params.propertyId !== undefined) {
            queryParameters.set('propertyId', <any>params.propertyId);
        }

        // to determine the Content-Type header
        let consumes: string[] = [
        ];

        // to determine the Accept header
        let produces: string[] = [
            'text/plain',
            'application/json',
            'text/json'
        ];

        // authentication (oauth2) required
        // oauth required
        if (this.configuration.accessToken) {
            let accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers.set('Authorization', 'Bearer ' + accessToken);
        }

        return callApiEndpoint(
            this.http, 
            path,
            headers,
            {
                method: RequestMethod.Get,
                headers: headers,
                search: queryParameters,
                withCredentials: this.configuration.withCredentials
            },
            Object.assign({}, this.configuration, $options),
            retryTimesToGo => {
                $options = $options || {};
                $options.retryTimes = retryTimesToGo;

                return this.financeAccountsSchemaGetWithHttpInfo(params, $options);
            }
        )
    }

}
