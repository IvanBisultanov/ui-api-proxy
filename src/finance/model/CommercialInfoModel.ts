/**
 * Apaleo Finance API
 * Everything around the Folio, Invoices and Accounting.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import * as models from './models';

import { FormBuilder, FormGroup }                                               from '@angular/forms';
import { ResponseModel }                                                        from '../../models';
import { getControl, adjustDefaultControls, prepareFormGroup }                  from '../../functions.model';
import { BuildFormOptions, ApaleoPropertyMetaData, ApaleoEnumPropertyMetaData } from '../../types';

export interface CommercialInfoModel {
    registerEntry?: string;

    taxId?: string;

    managingDirectors?: string;

}

export type CommercialInfoModelWithRawHttp = CommercialInfoModel & ResponseModel<CommercialInfoModel>;

export namespace CommercialInfoModel {
    export const $metaData = { 
        registerEntry: Object.freeze({ 
            type: 'string',
            isPrimitiveType: true,
        } as ApaleoPropertyMetaData),
        taxId: Object.freeze({ 
            type: 'string',
            isPrimitiveType: true,
        } as ApaleoPropertyMetaData),
        managingDirectors: Object.freeze({ 
            type: 'string',
            isPrimitiveType: true,
        } as ApaleoPropertyMetaData),
    };

    export function $buildForm(fb: FormBuilder, options?: BuildFormOptions<CommercialInfoModel>): FormGroup {
        const defaultControls = { 
            registerEntry: getControl($metaData.registerEntry, options, 'registerEntry'),
            taxId: getControl($metaData.taxId, options, 'taxId'),
            managingDirectors: getControl($metaData.managingDirectors, options, 'managingDirectors'),
        };
        const group = fb.group(adjustDefaultControls(defaultControls, options));
        prepareFormGroup(group, $metaData, options);

        return group;
    }
}

